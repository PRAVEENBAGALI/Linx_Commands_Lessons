cat command is used to is used to print the content of the file

cat ADMORE.log

if we need to add line numbers to the file then add -b
cat -b ADMORE.log

if we need to add line number to the blank line then add command as
cat -n ADMORE.log

if in the file we many blank lines and we need to show the content by sqeezing the content then use
cat -n ADMORE.log

cat commands

Other commands 


